export const metadata = {
  title: "All Appointments",
  description:
    "Browse all available doctors and book your appointment on DocAppoint.",
};

export default function AppointmentsLayout({ children }) {
  return children;
}