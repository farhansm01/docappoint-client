export const metadata = {
  title: "Register",
  description:
    "Create a new DocAppoint account to start booking doctor appointments.",
};

export default function RegisterLayout({ children }) {
  return children;
}