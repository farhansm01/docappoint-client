export const metadata = {
  title: "Dashboard",
  description:
    "Manage your bookings and profile on your DocAppoint dashboard.",
};

export default function DashboardLayout({ children }) {
  return children;
}